include:
  - services/compose.yml
  - networks.yml

services:
  main:
    build: ./main/
    hostname: efserver
    restart: unless-stopped
    networks:
      - mainet
    depends_on:
      - redis_server
      - webpage
    volumes:
      - "./static/:/home/ef/static"

  redis_server:
    image: redis
    hostname: redis_server
    restart: unless-stopped
    networks:
      - mainet
